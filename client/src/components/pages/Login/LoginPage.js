import React from "react";
import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <div className="bg-gradient-to-b from-violet-800 to-white">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
